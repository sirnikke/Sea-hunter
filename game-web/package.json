{
  "name": "sea-hunter-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Sea Hunter — Phase 1 vertical slice (Caribbean Sea + Young Kraken). Web-native TypeScript rail shooter prototype, playable on phone and desktop.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  },
  "dependencies": {
    "three": "^0.185.0"
  }
}
